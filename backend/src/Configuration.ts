export namespace Configuration {
    export const publicDir = __dirname + "/../public/"
    export const imageDir = __dirname + "/../public/images/"
    export const thumbnailDir = __dirname + "/../public/thumbnails/"
}
